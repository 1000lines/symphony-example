# Generated by Copier; edit the template source.
name: Cadence AI Review Trigger

on:
  pull_request_target:
    types: [review_requested, closed]
  workflow_dispatch:
    inputs:
      pr_number:
        description: PR number to review
        required: true
        type: string

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  review:
    uses: 1000lines/symphony-client-workflows/.github/workflows/cadence-ai-review-trigger.yml@05451a7c520e295c415012cf468ec24e6460bb48
    with:
      helpers-ref: 05451a7c520e295c415012cf468ec24e6460bb48
      pr_number: ${{ inputs.pr_number || format('{0}', github.event.pull_request.number) }}
    secrets:
      CADENCE_APP_PRIVATE_KEY: ${{ secrets.CADENCE_APP_PRIVATE_KEY }}
      CADENCE_OPENAI_API_KEY: ${{ secrets.CADENCE_OPENAI_API_KEY }}
      CADENCE_AI_REVIEW_ANTHROPIC_API_KEY: ${{ secrets.CADENCE_AI_REVIEW_ANTHROPIC_API_KEY }}
      CADENCE_LINEAR_API_TOKEN: ${{ secrets.CADENCE_LINEAR_API_TOKEN }}
