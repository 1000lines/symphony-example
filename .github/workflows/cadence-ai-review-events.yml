# Generated by Copier; edit the template source.
name: Cadence AI Review Events

on:
  workflow_run:
    workflows: [Cadence Review Ingress]
    types: [completed]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  review:
    uses: 1000lines/symphony-client-workflows/.github/workflows/cadence-ai-review-events.yml@05451a7c520e295c415012cf468ec24e6460bb48
    with:
      helpers-ref: 05451a7c520e295c415012cf468ec24e6460bb48
    secrets:
      CADENCE_APP_PRIVATE_KEY: ${{ secrets.CADENCE_APP_PRIVATE_KEY }}
      CADENCE_OPENAI_API_KEY: ${{ secrets.CADENCE_OPENAI_API_KEY }}
      CADENCE_AI_REVIEW_ANTHROPIC_API_KEY: ${{ secrets.CADENCE_AI_REVIEW_ANTHROPIC_API_KEY }}
      CADENCE_LINEAR_API_TOKEN: ${{ secrets.CADENCE_LINEAR_API_TOKEN }}
