# Generated by Copier; edit the template source.
name: Cadence Review Check Cleanup

on:
  workflow_run:
    workflows: [Cadence AI Review Events, Cadence AI Review Trigger, Cadence AI Review]
    types: [completed]

permissions:
  contents: read
  actions: read

jobs:
  cleanup:
    uses: 1000lines/symphony-client-workflows/.github/workflows/cadence-review-check-cleanup.yml@05451a7c520e295c415012cf468ec24e6460bb48
    with:
      helpers-ref: 05451a7c520e295c415012cf468ec24e6460bb48
    secrets:
      CADENCE_APP_PRIVATE_KEY: ${{ secrets.CADENCE_APP_PRIVATE_KEY }}
      CADENCE_LINEAR_API_TOKEN: ${{ secrets.CADENCE_LINEAR_API_TOKEN }}
